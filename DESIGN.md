# SoundTag

自分のPixelで、許可したスピーカーだけをNFCタグから操作する。
共通デザイン原本「rust-svelte-template / DESIGN.md」のSumi/Kinari・情報密度・操作規約を
2026-09-19にAndroid標準Viewへ適用した。この文書が製品の画面設計の正本。

## 設定と書込み

- 通常起動は「接続タグ」の機器一覧。機器名・接続状態・自動操作の許可を1つのまとまりにする。
  接続状態は文字で、許可はラベル付きスイッチで表示する。許可は接続操作ではない。
- 許可した機器名を押すと接続タグの書込みへ進む。未許可の機器は名前の選択を無効にする。
  長い名前は省略せず折り返す。許可更新・Bluetooth状態更新で一覧のスクロール位置を飛ばさない。
- 「全て切断」は機器一覧の次。自動操作を許可した機器だけが対象と分かる説明を付ける。
  許可した機器がなければ無効にする。通知設定はその下の補助操作に置く。
- 書込み画面は操作と対象→状態→主操作の順。内容の上書きを伝えてから開始する。
  待機・書込み中・成功・失敗をアイコンと短い日本語で伝え、失敗には再試行を用意する。
- 「中止」は書込みの準備画面へ、結果の「タグを離して戻る」は直接機器一覧へ戻る。
  システムの戻る操作も書込みを中止して一覧へ戻る。一覧での戻るはOS標準に従う。
  書込み中断時はタグの内容を確認するよう伝える。
- 画面離脱・再生成時は書込みを中止し、自動再開しない。古い結果で新しい操作を更新しない。
- 権限拒否・Bluetooth OFF・機器なし・関連付け失敗・NFC OFF/非搭載には理由と復帰操作を表示する。
  設定画面を開いただけではBluetoothの接続状態を変えない。NFC書込みでも変えない。

## 色と部品

OSの明暗設定に追従する。独自のテーマ切替は置かない。値はAndroid resourceへ集約する。

| 用途 | Kinari（明） | Sumi（暗） |
| --- | --- | --- |
| 背景・起動背景 | #faf6ef | #191919 |
| 機器・書込み面 | #fffdf8 | #232323 |
| 本文 | #3a2f28 | #e6e6e6 |
| 補助文字 | #6f6257 | #9a9a9a |
| 境界 | #e3d9c9 | #333333 |
| 操作・接続・成功 | #215e55 | #b4d5c2 |
| 強調面上の文字 | #ffffff | #191919 |
| 失敗 | #9c2b1d | #ff6b6b |

- 本文のコントラストは4.5:1以上。成功/失敗/接続を色だけに頼らず文字と組み合わせる。
- Android標準のButton/Switchとシステム書体。本文16sp、補助14sp、機器18sp、見出し22sp、アプリ名28sp。
- 余白は4/8/12/16/24dp。面は角丸8dp・境界1dp、影や大きな説明帯は加えない。
- 塗りつぶした主ボタンは書込み開始/再試行の最大1つ。機器一覧や戻るは中立色の部品。
- 操作領域は48dp以上。文字拡大に応じて高さが増え、全操作へスクロールで到達できる。
  system insetを避け、システムバーのアイコンも明暗へ追従する。
- 機器ごとの許可には名前を含む読み上げ説明、進行状況にはlive regionを付ける。
  状態アイコンは隣の状態文と重複して読み上げない。

## タグ読取りと通知

- タグ読取りでは画面を開かない。権限画面も自動では開かない。
- Bluetoothの接続状態が変わった場合だけ結果をAndroid標準通知で伝える。
  状態不変の反復・失敗は無通知。一部変更後の失敗は変化と失敗理由を通知する。
- 処理中のOS通知は低い重要度で表示を遅延し、処理終了時に取り除く。
- 通知タップで設定へ進む。許可・再設定は通常の設定画面で行う。
  ユーザーがOS側で設定した通知のサイレント等を上書きしない。
- v0.1.2のPixel 9ではOS側の結果通知をサイレントにした条件でYouTube表示維持を受入済み。
  新版のエミュレータ検証を実機でのNFC・Bluetooth・音声の検証と混同しない。

## 画面での確認

同じ複数機器・接続/許可の混在・長名・画面幅で変更前後を比較する。
明暗、320dp幅、2倍文字、空/権限拒否/Bluetooth OFF、書込み待機/成功/失敗/取消し/再生成を
実画面と操作で確認する。通知・認可・URIの既存mock回帰も維持する。
