# SoundTag

自分のPixelで、許可したスピーカーだけをNFCタグから操作する。

- 起動画面に「特定の機器の接続」と「全て切断」を置く。通常起動だけでは接続を変更しない。
- 機器のスイッチは自動操作の許可を表す。接続中の表示と混同しない。
- 許可した機器を選ぶと、その機器への接続タグを作成できる。
- 「全て切断」は自動操作を許可した機器だけを切断するタグを作る。
- 選択した操作・機器と上書きを確認し、「タグに書き込む」→タグをかざす→結果へ進む。
- 書込み待ち・書込中・成功・失敗をアイコンと短い日本語で伝える。失敗は再試行できる。
- 結果画面はタグを離してから戻る。書込み中の画面離脱では中断し、自動で再開しない。
- アイコンに読み上げ説明を付け、操作領域は48dp以上にする。
- 文字拡大でもスクロールして全操作へ到達でき、システムバーと内容を重ねない。
- 権限拒否、Bluetooth OFF、機器なし、関連付け失敗、NFC OFF・非搭載を空白画面にしない。
- タグ起動時は進行・成功・失敗のアイコンと短い結果を表示し、設定を開く／閉じるへ進める。
- 標準Android部品、白系の背景、濃い緑のアクセントを使う。通常画面へAPI名を出さない。
